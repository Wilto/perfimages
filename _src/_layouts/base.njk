<!doctype html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">

		<title>{{ metadata.title }}</title>
		<meta property="og:site_name" content="{{ metadata.title }}">

		<meta name="description" content="{{ metadata.description }}">

		<!-- Twitter Card Stuff: -->
		<meta name="twitter:card" content="summary_large_image">
		<meta name="twitter:site" content="@wilto">
		<meta property="og:site_name" content="{{ metadata.title }}">
		<meta property="og:title" content="{{ metadata.title }}">
		<meta property="og:url" content="https://wiltomakesfood.com{{page.url}}">
		<meta property="og:description" content="{{ lede }}">
		<meta property="og:image" content="https://matmarquis.com/_assets/img/book-crop-new.gif">
		<meta property="og:image:alt" content="Cover of Image Performance, published by A Book Apart, written by Mat Marquis, foreword by Ethan Marcotte.">
		<meta property="og:type" content="article">
		<meta name="author" content="Mat Marquis">

		<link rel="stylesheet" href="/_assets/css/style.css">
	</head>
	<body>
		<a href="#main" class="a11y-only focusable skiplink">Skip to Main Content</a>

		<div>
			<div class="topwrap">
			<header role="banner" class="site-head">
				<h1 class="site-hed">{{ title | safe }}</h1>
				<p class="byline">
					<span class="a11y-only">by:</span>
					{{ byline | safe }}
				</p>
				<span class="volume">{{ volume | safe }}</span>
			</header>
			{% if cover %}
				{% if href %}
				<a href="{{ href }}">
				{% endif %}
				<img class="cover" src="{{ cover }}" alt="Cover of {{ title | safe }} by {{ byline | safe }}.">
				{% if href %}
				</a>
				{% endif %}
			{% endif %}
		</div>

			<main role="main" id="main"{% if templateClass %} class="{{ templateClass }}"{% endif %}>
				{% if lede %}
				<div class="lede">
					<p>{{ lede | safe }}</p>
				</div>
				{% endif %}

				{{ layoutContent | safe }}
			</main>

			<footer class="site-foot">
				<div class="blurbs">
					{%- for intro in collections.blurb -%}
						{% include "partials/people-are-saying.njk" %}
					{%- endfor -%}

					{%- for blurb in collections.blurbs -%}
						{% include "partials/blurb.njk" %}
					{%- endfor -%}
				</div>

				{%- for bio in collections.bio -%}
					{% include "partials/bio.njk" %}
				{%- endfor -%}
			</footer>
		</div>
	</body>
</html>
