---
layout: base.njk
templateClass: tmpl-home
---

{% if title %}
<h2>{{ title | safe }}</h2>
{% endif %}

{% if lede %}
<div class="lede">
	{{ lede | safe }}
</div>
{% endif %}

{{ layoutContent | safe }}
