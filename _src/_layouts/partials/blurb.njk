<blockquote>
	{{ blurb.data.content | safe }}
	<cite>
		<a href="{{ blurb.data.site | url }}" class="blurb-author">{{ blurb.data.name | safe }}</a>
		<span class="title">{{ blurb.data.title | safe }}</span>
	</cite>
</blockquote>